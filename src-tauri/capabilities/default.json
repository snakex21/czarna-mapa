{
  "identifier": "default",
  "description": "Domyślne uprawnienia aplikacji",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "fs:default",
    "dialog:default"
  ]
}
